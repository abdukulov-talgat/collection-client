import React from 'react';
import { Divider, Grid } from '@mui/material';

const ItemShowDivider = () => {
    return (
        <Grid item xs={12}>
            <Divider />
        </Grid>
    );
};

export default ItemShowDivider;
